/**
 * Shared Domain Index
 * 
 * Common types and constants shared across domains.
 */

export type {
    ServiceLine,
    TechnicianOption,
    InventoryPart,
    SelectedInventoryLookup,
} from './types';

export { SERVICE_LINE_OPTIONS } from './types';
